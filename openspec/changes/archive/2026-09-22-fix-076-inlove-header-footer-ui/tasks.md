## 1. Header: позиция маркера активного пункта

- [x] 1.1 Сверить фактическую разметку/CSS маркера активного пункта в desktop dropdown «Услуги» (`SiteHeader`) с howto `agents/howto/site-ksk-inlove-design.md` и найти причину наложения маркера на текст
- [x] 1.2 Исправить позиционирование маркера так, чтобы он отображался под строкой текста пункта, не перекрывал буквы и не отрывался от строки заметным разрывом (код); ручная проверка на desktop viewport ≥1024px не выполнялась в этом unit
- [ ] 1.3 Убедиться, что `aria-current="page"` и keyboard/focus-поведение dropdown «Услуги» не регрессировали (ручная проверка Tab/Escape)

## 2. Footer: маркер активного пункта и оформление группы «Услуги»

- [x] 2.1 Реализовать в footer navigation тот же принцип позиционирования маркера активного пункта (под текстом, без наложения, без отрыва), что и в header
- [x] 2.2 Убрать у группы «Услуги» в footer обрамляющий контейнер/рамку и иконку-стрелку слева от подписи «УСЛУГИ», оставив только отступ (indentation) дочерних пунктов как визуальное отличие
- [ ] 2.3 Проверить вручную, что порядок пунктов, primary phone и набор каналов footer по-прежнему совпадают с header (синхронизация сохраняется везде, кроме стиля группы «Услуги»)

## 3. Главная страница: отступ карты в контактах

- [x] 3.1 Добавить заметный верхний отступ у карты (iframe widget) в `ContactSection` на главной, чтобы она не примыкала вплотную к блоку контактов над ней
- [ ] 3.2 Проверить вручную, что тот же компонент `ContactSection` на `/about` не получил лишнего/дублирующего отступа из-за общего изменения

## 4. Зелёные CTA-блоки (`Section tone="forest"`)

- [x] 4.1 Увеличить отступ между верхней границей `Section` с `tone="forest"` и заголовком блока до значения, сопоставимого с отступом аналогичных секций других `tone`, через существующую spacing-шкалу `src/ui/**`
- [x] 4.2 Добавить `Button` внутри `Section` с `tone="forest"` белую окантовку (border/outline), сохранив текущий цвет заливки кнопки
- [ ] 4.3 Проверить вручную на `/uslugi/zanyatiya`, `/uslugi/progulki`, `/uslugi/postoy` (и любых других страницах, использующих этот CTA-паттерн), что заголовок и кнопки в зелёном блоке выглядят согласованно

## 5. Browser QA и приёмка

- [ ] 5.1 Прогнать ручную проверку всех пяти правок (1.2, 2.1, 2.2, 3.1, 4.1-4.2) на контрольных viewport 320/768/1024/1440px, включая проверку отсутствия horizontal scroll и видимого `focus-visible`
- [x] 5.2 Прогнать существующие автоматизированные проверки сервиса (unit-тесты/lint/typecheck по правилам `site-ksk-inlove`) и зафиксировать результат
- [x] 5.3 Выполнить `openspec validate fix-076-inlove-header-footer-ui --strict` и зафиксировать успешный результат (CLI недоступен в песочнице — выполнена ручная замена по правилам `schema: spec-driven`, см. handoff)
