# Development Report: 

**Статус:** `<READY_FOR_QG | REWORK | APPROVED>`
**Дата:** `YYYY-MM-DD`
**Рекомендуемая ветка:** `<branch-name>`

## Ссылки

- План: `[<path/to/plan.md>](../plans/<plan-file>.md)`
- Задача: `[<path/to/task.md>](../tasks/<task-file>.md)` 

## Краткий контекст

Коротко опиши, что было сделано и почему. Текста должно быть достаточно, чтобы следующий агент мог продолжить работу без повторного анализа всего diff.

## Измененные файлы


| Файл             | Что изменено         |
| ---------------- | -------------------- |
| `<path/to/file>` | `<краткое описание>` |


## Unit / Integration тесты


| Команда     | Результат                 | Примечание                                          |
| ----------- | ------------------------- | --------------------------------------------------- |
| `make test` | `<passed/failed/not run>` | `<X passed, Y failed; причина если не запускалось>` |


## SMOKE-тесты

Перед заполнением этой секции Quality Gate обязан прочитать `.claude/skills/api-smoke-test/SKILL.md` и выполнить SMOKE-тесты по этому skill.


| #       | Endpoint   | Method | HTTP  | Time     | Результат | Примечание              |
| ------- | ---------- | ------ | ----- | -------- | --------- | ----------------------- |
| `SM-01` | `/api/...` | `GET`  | `200` | `123 ms` | `passed`  | `<проверенное условие>` |


Итог SMOKE: `<N/M passed>`.

## Замечания и риски

- `<нет / список известных ограничений>`

## Rework checklist

### Backend

- `<backend action или удалить пункт, если не требуется>`

### Frontend

- `<frontend action или удалить пункт, если не требуется>`

### Quality Gate

- Повторно запустить unit/integration тесты
- Повторно запустить SMOKE-тесты и указать endpoint timings

