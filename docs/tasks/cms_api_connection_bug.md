# Контекст
Мы выполняли следующую задачу:
- `tasks/equestrian_entity`
- `plans/feature/equestrian_entity`
- `reports/FEATURE-equestrian-entity-review`
В результате выполнения мы реализовали логику, что cms-consumer должен обязательно отправлять заголовок с указанием конюшни
Однако при деплое на прод
- `services/site-ad/.github/workflows/check_and_deploy.yml`
- `services/site-ad/.helm`
Получилось так, что consumer не выполняет никакие запросы к бэкэнду CMS и не загружает никаких данных
В github есть следующие секреты:

| Наименование                       | Значение                 |
| ---------------------------------- | ------------------------ |
| EQUESTRIAN_SERVICE_KEY             | default-equestrian       |
| NEXT_PUBLIC_EQUESTRIAN_SERVICE_KEY | default-equestrian       |
| NEXT_PUBLIC_API_BASE_URL           | https://api.eqcms.ru/api |
Они нужны для сборки Docker образа
# Что нужно сделать
## Избавиться от одной из переменных
Две переменных согласно .env.exmaple содержат одно и то же значение
Необходимо избавиться от одной из переменных
## Найти и исправить баг
В результате должно получиться так, что развёрнутый consumer обращается за данными к https://api.eqcms.ru/api
## Подумать о возможности управления через секреты
На данный момент эти переменные можно задать исключительно через сборку образа. После сборки их нельзя отредактировать. Попробуй продумать возможность управления этими переменными через enviroment образа
# Задача
Напиши в docs/plans/bugfix/cms_api_connection_bug.md план по реализации решения этого бага
Не приступай к реализации до согласования плана
Ты можешь использовать `kubectl` команды для поиска причины бага, но не можешь выполнять никаких действий с объектами k8s