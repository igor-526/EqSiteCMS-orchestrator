# Контекст
На данный момент галерея работает как примонтированный к контейнеру том
Я хочу переработать эту логику под S3 хранилище
# Контейнеризация
Давай использовать в dev окружении и оркестрации minio s3 совместимое хранилище
Добавь в compose файл инфры такой контейнер. Продумай хранение Env
# Работа с галереей
Давай планировать отсутствие изменений на frontend зонах. Контракты оставляем, работаем с S3 только в рамках backend
Сохранять все фотографии будем в бакет "gallery"
Посмотри везде, где происходит отдача фотографий (лошади, прайсы и тд)
Нам нужно продумать, как конфигурировать ссылки фотографий, отправляемые в API, чтобы по ним успешно происходила загрузка

Учти, что на сервере фйалы хранятся в `src/media/`, а тут почему то в `storage/media/`
# Одноразовый скрипт на выгрузку
Нужен одноразовый скрипт, который переместит примонтированную папку в bucket и поправит все данные в БД для бесшовного продолжения работы с Minio
# Задача
Напиши в docs/plans/feature/s3-storage.md план по реализации этого функционала
План по тестированию продумаем после согласования плана изменений (по явной команде)
Не приступай к реализации до согласования плана