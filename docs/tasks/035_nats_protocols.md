# Контекст
В задачах `031_NATS_Jetstream_protocols` и `033_celery-protocols-and-redis` мы реализовывали протоколирование использования технолоний
При использовании Celery меня посетила идея фиксировать это в README.md и в отдельном файле параметры  использования
# Задача
Давай для NATS тоже введём правила. Если сервис использует NATS Jetstream, то его README.md должен содержать информацию о том, какие стримы, субъекты и прочее используются и для чего. Что отправляем, что принимаем
Дополни протокол этим правилом, посмотри сервисы `backend`, `notification-service`, `email-service` и доработай README.md этих сервисов