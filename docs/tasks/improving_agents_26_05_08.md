# Контекст
Нам нужно доработать агента planner и backend для предотвращения инцидентов
# Issue 1. Smoke tests
Был реализован план docs/plans/feature/price_group_ordering.md
В результате реализации были написаны SMOKE тесты. Их нет необходимости писать. Все SMOKE тестирования всегда должны проходить на живом поднятом API через скилл .claude/skills/api-smoke-test
# Issue 2. Flake 8 exceptions
Правила flake8 конфликтуют с isort и black. Нам нужно добавить в исключения появление следующих строк:
```plaintext
./maintain/horse_generator/generate_horses.py:57:1: W293 blank line contains whitespace
./maintain/horse_generator/generate_horses.py:60:1: E303 too many blank lines (3)
```
# Issue 3. Flake 8 и ruff checks
После работы backend агент обязан из корня проекта выполнить команду make format, make test и make lint. Отдавать на Quality Gate только при отсутствии проблем в этих командах. QG при проверке тоже обязан выполнить эти команды и проверить чистоту результата
# Issue 4. Skipped tests
Команда `make test` возвращает следующий результат
```plaintext
...
tests/unit/core/services/test_horse_owner_service.py .................s.............................s......sss..................... [ 49%]
...
===================================================== 413 passed, 5 skipped in 7.64s ======================================================
```
# Задача
В docs/plans/agents/improving_agents_26_05_08.md составить план по исправлению всех указанных Issues в соответствии с пунктами ниже
Не писать никакого кода до согласования плана. Составить только план правок согласно следующим пунктам:
- Провести smoke тестирование, которое было реализовано в виде скриптов при реализации плана docs/plans/feature/price_group_ordering.md
- Удалить файлы smoke тестов
- Проанализировать и скорректировать агентов таким образом, чтобы этот инцидент больше не повторялся. Все smoke тесты только на живом API и через скилл
- Спланировать изменения, указанные в Issue 2
- Доработать агентов в соответствии с Issue 3
- Отразить в плане причины Issue 4 и предложения, что с этим сделать
- Необходимо сейчас исправить все ошибки, возникающие в результате выполнения команды `make lint`