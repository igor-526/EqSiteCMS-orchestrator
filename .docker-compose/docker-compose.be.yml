services:
  backend:
    build:
      context: ../services/backend
      dockerfile: Dockerfile
    image: eqsitecms-backend:latest
    container_name: eqsitecms-app
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - path: ../services/backend/.env
        required: true
    environment:
      LOGS_DIR: /eqsitecms/logs
      # Canonical CMS is published on 3001; keep 3000 for the documented
      # standalone development server.
      CMS_PANEL_DOMAIN: ${CMS_PANEL_DOMAIN:-localhost:3001}
      CMS_CORS_ORIGINS: ${CMS_CORS_ORIGINS:-http://localhost:3000,http://localhost:3001}
    ports:
      - "${EXPOSE_APP_PORT}:8000"
    volumes:
      - ../services/backend/src:/eqsitecms/src:${DEV_MOUNT:-ro}
      - ../services/backend/scripts:/eqsitecms/scripts:ro
      - ../services/backend/logs:/eqsitecms/logs
    working_dir: /eqsitecms
    command: ["uv", "run", "src/main.py"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health').read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - eqsitecms_network
  # migration:
  #   build:
  #     context: ../services/backend
  #     dockerfile: Dockerfile
  #   image: eqsitecms-backend-migration:latest
  #   restart: "no"
  #   env_file:
  #     - path: ../services/backend/.env
  #       required: true
  #   volumes:
  #     - ../services/backend/src:/eqsitecms/src:${DEV_MOUNT:-ro}
  #   working_dir: /eqsitecms/src
  #   command: >
  #     sh -c 'if [ "$$SKIP_MIGRATIONS" = "true" ]; then
  #       echo "Миграции пропущены (SKIP_MIGRATIONS=true)";
  #       exit 0;
  #     else
  #       uv run alembic -c alembic.ini upgrade head;
  #     fi'
  #   networks:
  #     - eqsitecms_network

networks:
  eqsitecms_network:
    external: true
